package JuegoDamasChinas;

import java.io.*;

public class PosiciónLibre 
{
    public static int[] pos_posición_libre_(Board board)
    {
    	 int f1 = 3, c1 = 4; // esquina inferior izquierda
         int f2 = 3, c2 = 7; // esquina inferior derecha
         int max = 0, dist1, dist2;
         int[] pos = new int[2]; //posicion a devolver
         
         for (i = 0; i < rows; i++)
         {
        	 for (j = 0; j = columns; j++)
        	 {
        		 print m [i][j];
        	 }
         }
         if (m[i - j] [i - j] = 0)
         {
        	 return "posicion libre"
         }
    }
}
        
